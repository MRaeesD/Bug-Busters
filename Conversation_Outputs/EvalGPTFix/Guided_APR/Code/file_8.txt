else if (t == 2) {
    int size = que.size(); // Store size before polling
    int x = que.poll();
    long s = (x % mod) * power(10, size - 1, mod); // Use original size
    ans = ans - s + mod;
    ans %= mod;
} 